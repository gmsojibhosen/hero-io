import React from 'react';
import { ToastContainer} from 'react-toastify';
const Root = () => {
    return (
        <div>
            <ToastContainer />
        </div>
    );
};

export default Root;